# Висновки щодо ефективності алгоритмів сортування

У порівнянні розглядаються два типи масивів: - масив, відсортований у
зворотному порядку; - масив, елементи якого розподілені випадково.

Нижче наведено аналіз ефективності алгоритмів та узагальнене порівняння,
який із них найшвидший.

------------------------------------------------------------------------

## 🔹 Insertion Sort (сортування вставками)

**Найгірший випадок - масив у зворотному порядку.**

Причини: - кожен елемент рухається через увесь відсортований префікс; -
максимальна кількість порівнянь і зсувів; - повна квадратична
складність.

**Случайний масив працює дещо краще, але все одно O(n²).**

**Підсумок:**\
\> Insertion sort - найповільніший із трьох алгоритмів на реальних
великих масивах.

------------------------------------------------------------------------

## 🔹 Merge Sort (сортування злиттям)

Merge sort виконує рекурсивне поділ + злиття незалежно від розташування
елементів.

-   структура даних майже не впливає на швидкість;
-   час виконання стабільний: **O(n log n)**;
-   завжди прогнозований.

**Підсумок:**\
\> Merge sort - значно швидший за insertion sort, стабільний і
передбачуваний.

------------------------------------------------------------------------

## 🔹 Timsort (алгоритм Python `.sort()` та `sorted()`)

Timsort - найоптимізованіший серед трьох.

Особливості: - знаходить монотонні послідовності (зростаючі або спадні); -
використовує insertion sort для маленьких шматків; - використовує merge
sort для злиття послідовностей; - спеціально оптимізований для реальних масивів.

### Важливо:

-   **Зворотний масив** перетворюється в одну велику послідовність після
    перевороту → дуже швидко.
-   **Випадковий масив** має короткі послідовності, але Timsort все одно працює
    швидше за класичний merge sort завдяки оптимізаціям.

**Підсумок:**\
\> Timsort - найшвидший алгоритм серед розглянутих.

------------------------------------------------------------------------

# 📌 Загальне порівняння швидкості

| Алгоритм        | Середня швидкість   | Худший випадок                | Коментар                                 |
|-----------------|----------------------|--------------------------------|-------------------------------------------|
| **Timsort**     | 🥇 Найшвидший        | Дуже хороший навіть у гірших випадках  | Оптимізований, використовує структуру даних |
| **Merge Sort**  | 🥈 Швидкий           | Стабільний                    | Чиста O(n log n), без оптимізацій         |
| **Insertion Sort** | 🥉 Дуже повільний | Вкрай поганий на великих N    | Лише для маленьких масивів               |


------------------------------------------------------------------------

# 🏁 Остаточний висновок: який алгоритм найкращий?

### **1. Найшвидший → Timsort**

-   оптимізації під реальні дані;
-   найкраща продуктивність на будь-яких масивах;
-   використовується в Python як стандарт.

### **2. Другий → Merge Sort**

-   швидкий, стабільний, рівномірний час виконання;
-   програє Timsort через відсутність оптимізації під runs.

### **3. Найповільніший → Insertion Sort**

-   тільки для маленьких та майже відсортованих масивів;
-   повністю програє іншим на великих n.
